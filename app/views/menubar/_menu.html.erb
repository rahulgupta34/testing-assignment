<nav class="navbar navbar-expand-lg navbar-light bg-light">
<%= link_to "Products", root_path, class:"navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse " id="navbarSupportedContent">
 
    <ul class="navbar-nav me-auto">
      <% if current_user %>
             <li class="nav-item">
                <%= button_to "Logout", destroy_user_session_path, class:"nav-link btn btn-success", method: :delete %>
            </li>

            <li class="nav-item">
                <%= link_to "Products", products_path, class:"nav-link" %>
            </li>

            <% if current_user.role == "admin" %>
                    <li class="nav-item">
                        <%= link_to "New product", new_product_path, class:"nav-link" %>
                    </li>

                     <li class="nav-item">
                        <%= link_to "Delete", "/delete", class:"nav-link" %>
                    </li>

            <% end %>
        <% else %>
            <li class="nav-item">
              <%= link_to "Login", user_session_path, class:"nav-link" %>
            </li>

            <li class="nav-item">
                <%= link_to "Signup", new_user_registration_path, class:"nav-link" %>
            </li>

             

      <% end %>
    </ul>
  </div>
</nav>
